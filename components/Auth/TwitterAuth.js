import React from "react";
import { View, Text } from "react-native";
import Loader from "../Loader";

export default TwitterAuth = ({ getSNSInfo, loading }) => {
  if (loading) {
    return <Loader />;
  }
};
